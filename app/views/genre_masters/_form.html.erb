<%= form_with(model: genre_master) do |form| %>
  <% if genre_master.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(genre_master.errors.count, "error") %> prohibited this genre_master from being saved:</h2>

      <ul>
        <% genre_master.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :genre_id, style: "display: block" %>
    <%= form.number_field :genre_id %>
  </div>

  <div>
    <%= form.label :genre_name, style: "display: block" %>
    <%= form.text_field :genre_name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
